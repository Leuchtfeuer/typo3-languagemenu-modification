# This is an example configuration
config.tx_languagemod {
    # language uids the modification should handle
    languages = {$config.tx_languagemod.languages}

    # page uids the modification should handle
    pages = {$config.tx_languagemod.pages}

    params {
        # table name = name of the get variable. For example: tx_news_pi1[news]
        tx_news_domain_model_news = tx_news_pi1.news
    }
}
